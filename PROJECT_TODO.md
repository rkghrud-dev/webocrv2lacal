# Project TODO

## Coupang 1,000 Won 이하 업로드 처리

- 쿠팡 신규 API 업로드 배송 정책은 `유료배송 3,000원 / 50,000원 이상 무료배송`으로 고정한다.
  - 등록 JSON 최종값: `deliveryChargeType=CONDITIONAL_FREE`, `deliveryCharge=3000`, `freeShipOverAmount=50000`
  - 참조상품 배송 템플릿이 무료배송이어도 업로드 직전에 위 값으로 덮어쓴다.
- 2026-05-27 확인: API 신규등록은 위 배송 정책 적용 시 판매가 900원 상품 등록 성공.
  - 테스트 상품: `GS0101244A`, A마켓, 판매가 900원
  - 쿠팡 응답: `SUCCESS`, `sellerProductId=16226940823`
- 2026-05-27 확인: 쿠팡 V4.6 엑셀 업로드도 판매가 1,000원 미만은 서버 검증에서 차단된다.
  - 오류: `최소 설정 가격은 1000원 입니다. (판매자배송 상품의 경우, 배송비가 포함된 가격입니다.)`
  - 가격변경, 쿠폰/할인, 엑셀 업로드는 1,000원 미만 처리 불가로 본다.
- 쿠팡 API 신규등록에서는 판매가 1원 이상 상품을 업로드 대상으로 허용한다.
- 제외 상품은 쿠팡 V4.6 엑셀 업로드 양식으로 별도 생성한다.
- 엑셀 양식 생성 시 다음을 반드시 맞춘다.
  - 카테고리 셀은 `[카테고리ID] 전체>카테고리>경로` 형식
  - 카테고리별 필수 구매옵션/검색옵션은 `coupang_categories.csv` 기준 적용
  - 이미지 셀은 로컬 경로가 아니라 Cafe24/ESM 공개 URL 사용
  - 가격/옵션/이미지/고시정보 검증 실패 파일을 재분석해 보정

## 카테고리 강검증 개편

- `부속`, `부품`, `액세서리` 같은 넓은 단어만으로 카테고리를 확정하지 않는다.
- 상품명, OCR 텍스트, 옵션, 상세 설명을 함께 보고 카테고리 후보를 재평가한다.
- 네이버/쿠팡/롯데ON 카테고리 코드와 경로를 모두 저장한다.
- 의미 충돌 예시를 업로드 전 자동 탐지한다.
  - `분무기/원예/농업/필터/거름망` 상품이 `나사/앙카`로 매칭되는 경우
- 이상탐지 상품은 API 업로드 제외 후 검수 리포트로 분리한다.
- 기존 업로드 상품은 가능한 마켓부터 API로 카테고리를 수정한다.

## 기존 업로드 상품 카테고리 API 수정

- 네이버는 `originProductNo` 기준 `leafCategoryId` 수정 성공 확인.
- `GS0101244A` 네이버 A/B 상품은 `50003466`에서 `50003372`로 수정 완료.
- 쿠팡 기존 승인상품 카테고리 수정 가능 여부는 API/엑셀 결과로 분리 확인한다.
- API 수정이 막히는 마켓은 엑셀 수정 또는 수동 작업 리스트로 분리한다.

## Cafe24ShipmentManager 상품코드 누락 조사

- 대상 프로젝트: `C:\Users\rkghr\Desktop\프로젝트\Cafe24ShipmentManager`
- 문제: 구글 시트에 붙여넣는 출고용 내용 생성 시 상품코드가 비는 경우가 많다.
- 현재 상품코드는 Cafe24 API와 쿠팡 API 주문 데이터를 기반으로 추출한다.
- 핵심 조사 위치:
  - `MainForm.OrderExport.cs`
  - `BuildOrderExportRowsEx`
  - `BuildCoupangRow`
  - `ResolveCoupangProductCode`
  - `ResolveBaseProductCode`
  - `ExtractProductCode`
  - 직접입력 매핑 저장/조회 로직
- 조사할 원인:
  - Cafe24 주문 item의 `custom_product_code`가 비어 있는 경우
  - 쿠팡 주문 item의 `externalVendorSkuCode`가 비어 있는 경우
  - 상품명/옵션명에 GS 코드가 없거나 변형되어 정규식 추출 실패
  - `vendorItemId`, `sellerProductId`, `shipmentBoxId`와 내부 GS 코드 매핑 테이블 부재
  - 직접입력 매핑 키가 상품명/옵션 텍스트 변화에 취약한 경우
- 개선 방향:
  - API 원문 JSON을 실패 케이스별로 저장하고 누락 원인을 분류한다.
  - `vendorItemId -> GS코드`, `sellerProductId -> GS코드`, `상품명+옵션 -> GS코드` 보조 매핑을 만든다.
  - WEBOCR 업로드 이력과 Coupang/Naver 업로드 로그에서 상품코드 매핑을 재사용한다.
  - 상품코드 미해결 주문은 구글 붙여넣기 전에 별도 리포트/수동 매핑 큐로 분리한다.
  - 수동 입력된 매핑은 옵션명 변형에도 재사용되도록 정규화 키를 강화한다.
